# Default: all text files use LF in the repository and in the working tree.
# `text=auto` lets git auto-detect binaries, so binary files are unaffected.
* text=auto eol=lf

# Windows batch files must use CRLF
*.bat text eol=crlf

# Shell scripts must use LF
*.sh text eol=lf
